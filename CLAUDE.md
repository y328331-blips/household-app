# household-app

## Repository
- GitHub (public): https://github.com/y328331-blips/household-app.git
- Remote name: `origin`

## Workflow rules
- Always ask for explicit confirmation before running `git add`/`git stage`, `git commit`, or `git push` in this repo. Do not run these commands proactively.
- This repo is public. Never commit secrets, credentials, or the local SQLite data file (`household.db`, already in `.gitignore`).
